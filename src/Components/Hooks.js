/*import React, { useState } from 'react'

export default function Hooks() {
let sColor="purple"

    const [emo,setemo]=useState("Click Me")
    const [color,setColor]=useState(sColor)
    
  const handler =()=>{
      let color2="red"
    setemo("osama Here")
    setColor(color2)
   
  }
  const Back =()=>{
      setemo("Click Me")
      setColor(sColor)
  }

    return (
        <div className="main" style={{backgroundColor:color}}>
            
            <button onClick={handler} onDoubleClick={Back}>{emo}</button>
        </div>
    )
}
*/